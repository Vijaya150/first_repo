pipeline {
    agent testlabel

    stages {
        stage('Build') {
            steps {
                sh 'Build success'
            }
        }

        stage('Test') {
            steps {
                sh 'test suceess'
            }
        }

        stage('Deploy') {
            steps {
                sh 'Deploy success'
            }
        }
    }
}

